x-logging: &default-logging
  logging:
    driver: local
    options:
      max-file: 3
      max-size: 10m

services:
  forgejo:
    <<: *default-logging
    image: codeberg.org/forgejo/forgejo:16.0.5
    container_name: forgejo
    environment:
      - FORGEJO__SERVER__LANDING_PAGE=explore
      - FORGEJO__SERVICE__DISABLE_REGISTRATION=true
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /srv/forgejo/data:/data
    restart: unless-stopped
    labels:
      - glance.icon=sh:forgejo-light
      - glance.name=Forgejo
      - glance.url=https://git.ocd.beauty
      - traefik.enable=true
      - traefik.http.routers.gitea.entrypoints=websecure
      - traefik.http.routers.gitea.rule=Host(`git.ocd.beauty`)
      - traefik.http.routers.gitea.tls.certresolver=letsencrypt
      - traefik.http.services.gitea.loadbalancer.server.port=3000

networks:
  default:
    external: true
    name: proxy
