db:
  host: photos-postgres
  name: ${POSTGRES_DB}
  user: ${POSTGRES_USER}
  password: ${POSTGRES_PASSWORD}

s3:
  b2-eu-cen:
    endpoint: garage:3900
    bucket: b2-eu-cen
    key: ${GARAGE_DEFAULT_ACCESS_KEY}
    secret: ${GARAGE_DEFAULT_SECRET_KEY}
    use_path_style: true
